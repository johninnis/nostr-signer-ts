{
  "name": "@innis/nostr-signer",
  "version": "0.1.0",
  "publish": {
    "exclude": ["tests/", "deno.lock", ".github/"]
  },
  "license": "MIT",
  "description": "Browser-side signer acquisition for Nostr web applications: one descriptor names where the key lives (NIP-07 extension or NIP-46 bunker), one factory turns it into a Signer, storage remembers it between visits, and a NIP-98 proof builder turns the signer into an Authorization header.",
  "exports": {
    ".": "./mod.ts",
    "./session": "./session.ts"
  },
  "compilerOptions": {
    "strict": true,
    "noUncheckedIndexedAccess": true
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^1",
    "@innis/nostr-core": "jsr:@innis/nostr-core@^0.2.16",
    "@innis/nostr-core/testing": "jsr:@innis/nostr-core@^0.2.16/testing",
    "@innis/nostr-nip07": "jsr:@innis/nostr-nip07@^0.2.3",
    "@innis/nostr-nip46": "jsr:@innis/nostr-nip46@^0.2.0",
    "@innis/nostr-relay-pool": "jsr:@innis/nostr-relay-pool@^0.1.4"
  },
  "tasks": {
    "check": "deno check mod.ts session.ts src/ tests/",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check",
    "test": "deno test -A",
    "coverage": "rm -rf cov_profile && deno test -A --coverage=cov_profile && deno coverage cov_profile --lcov --output=cov_profile/lcov.info && deno coverage cov_profile && deno run --allow-read jsr:@innis/coding-standards@^0.1.0/check-coverage",
    "exports-tested": "deno run --allow-read --allow-run jsr:@innis/coding-standards@^0.1.0/check-exports-tested",
    "docs": "deno run --allow-read --allow-run jsr:@innis/coding-standards@^0.1.0/check-docs",
    "publish:dry": "deno publish --dry-run",
    "ci": "deno task fmt:check && deno task lint && deno task check && deno task coverage && deno task exports-tested && deno task docs"
  },
  "lint": {
    "plugins": [
      "jsr:@innis/coding-standards@^0.1.0/lint-plugin"
    ],
    "rules": {
      "include": [
        "no-console",
        "ban-untagged-todo",
        "explicit-function-return-type",
        "no-non-null-assertion"
      ],
      "exclude": [
        "require-await"
      ]
    },
    "exclude": [
      "cov_profile/"
    ]
  },
  "fmt": {
    "semiColons": false,
    "lineWidth": 120,
    "exclude": [
      "**/*.md",
      "cov_profile/"
    ]
  }
}
